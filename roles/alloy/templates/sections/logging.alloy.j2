// -------------------------------------------------------------------------
// Logging (Alloy's own internal log level)
// -------------------------------------------------------------------------

// Configures Alloy's own log output. This does not affect collected workload logs.
logging {
  level  = "info"
  format = "logfmt"
}
