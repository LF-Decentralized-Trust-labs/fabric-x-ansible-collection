#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#

- name: Delete Block Explorer Secret
  kubernetes.core.k8s:
    state: absent
    api_version: v1
    kind: Secret
    name: "{{ block_explorer_k8s_resource_name }}"
    namespace: "{{ k8s_namespace | default('default') }}"
