#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#

- name: Start Fabric-X Orderer consensus nodes
  ansible.builtin.include_role:
    name: hyperledger.fabricx.orderer
    tasks_from: "{{ orderer_deployment_mode }}/start"
  when: orderer_component_type == 'consensus'

- name: Start Fabric-X Orderer batcher nodes
  ansible.builtin.include_role:
    name: hyperledger.fabricx.orderer
    tasks_from: "{{ orderer_deployment_mode }}/start"
  when: orderer_component_type == 'batcher'

- name: Start Fabric-X Orderer assembler nodes
  ansible.builtin.include_role:
    name: hyperledger.fabricx.orderer
    tasks_from: "{{ orderer_deployment_mode }}/start"
  when: orderer_component_type == 'assembler'

- name: Start Fabric-X Orderer router nodes
  ansible.builtin.include_role:
    name: hyperledger.fabricx.orderer
    tasks_from: "{{ orderer_deployment_mode }}/start"
  when: orderer_component_type == 'router'
