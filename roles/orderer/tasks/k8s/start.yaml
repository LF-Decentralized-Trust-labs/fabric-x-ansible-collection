#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#

# =========================
# Namespace
# =========================
- name: Ensure Kubernetes namespace exists for Fabric-X Orderer
  ansible.builtin.include_role:
    name: hyperledger.fabricx.k8s
    tasks_from: namespace/create

# =========================
# Apply Resources
# =========================
- name: Apply Fabric-X Orderer Service
  kubernetes.core.k8s:
    state: present
    template:
      path: k8s/service.yaml.j2

- name: Apply Fabric-X Orderer NodePort Service
  kubernetes.core.k8s:
    state: present
    template:
      path: k8s/service-nodeport.yaml.j2
  when: orderer_k8s_rpc_node_port is defined or
    orderer_k8s_metrics_node_port is defined

- name: Apply Fabric-X Orderer LoadBalancer Service
  kubernetes.core.k8s:
    state: present
    template:
      path: k8s/service-loadbalancer.yaml.j2
  when: orderer_k8s_loadbalancer_expose_rpc_port or
    orderer_k8s_loadbalancer_expose_metrics_port

- name: Apply Fabric-X Orderer StatefulSet
  kubernetes.core.k8s:
    state: present
    template:
      path: k8s/statefulset.yaml.j2
    wait: true
    wait_timeout: "{{ orderer_k8s_wait_timeout }}"
