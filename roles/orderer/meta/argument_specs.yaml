#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#
---
argument_specs:
  role-options:
    short_description: Shared option catalog for orderer role entry points
    options:
      remote_config_dir: &remote_config_dir
        type: path
        required: true
        description:
          - "Shared base directory for generated configuration."
      remote_data_dir: &remote_data_dir
        type: path
        required: true
        description:
          - "Shared base directory for persisted runtime data."
      actual_host: &actual_host
        type: str
        required: true
        description:
          - "Host name or IP used by control-node HTTP requests."
      channel_id: &channel_id
        type: str
        required: true
        description:
          - "Channel identifier used to derive the genesis block filename."
      configtxgen_artifacts_dir: &configtxgen_artifacts_dir
        type: path
        required: true
        description:
          - "Control-node directory containing configtxgen artifacts."
      fetched_artifacts_dir: &fetched_artifacts_dir
        type: path
        required: true
        description:
          - "Control-node directory containing fetched crypto artifacts."
      cryptogen_artifacts_dir: &cryptogen_artifacts_dir
        type: path
        required: true
        description:
          - "Control-node directory containing cryptogen-generated crypto artifacts."
      k8s_namespace: &k8s_namespace
        type: str
        description:
          - "Kubernetes namespace used for orderer resources."
      k8s_storage_size: &k8s_storage_size
        type: str
        required: true
        description:
          - "PVC storage request used by the orderer StatefulSet."
      orderer_hosts: &orderer_hosts
        type: list
        elements: str
        required: true
        description:
          - "Inventory hosts dedicated to orderer nodes."
      orderer_registry_endpoint: &orderer_registry_endpoint
        type: str
        required: false
        default: "{{ lookup('env', 'ORDERER_REGISTRY_ENDPOINT') or 'docker.io/hyperledger' }}"
        description:
          - "Registry prefix used to build the orderer image reference."
          - "The default derives from C(ORDERER_REGISTRY_ENDPOINT)."
      orderer_image_name: &orderer_image_name
        type: str
        required: false
        default: fabric-x-orderer
        description:
          - "Image name used for the orderer container."
      orderer_image_tag: &orderer_image_tag
        type: str
        required: false
        default: "0.0.23"
        description:
          - "Image tag used for the orderer container."
      orderer_image: &orderer_image
        type: str
        required: false
        default: "{{ orderer_registry_endpoint }}/{{ orderer_image_name }}:{{ orderer_image_tag }}"
        description:
          - "Full image reference used by the container and Kubernetes branches."
          - "The default derives from C(orderer_registry_endpoint), C(orderer_image_name), and C(orderer_image_tag)."
      orderer_container_name: &orderer_container_name
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - "Container name used by the container lifecycle branch."
          - "The default derives from C(inventory_hostname)."
      orderer_git_hub_url: &orderer_git_hub_url
        type: str
        required: false
        default: github.com
        description:
          - "Git host used to resolve the orderer source repository."
      orderer_git_repo: &orderer_git_repo
        type: str
        required: false
        default: hyperledger/fabric-x-orderer
        description:
          - "Repository path for the orderer source code."
      orderer_git_commit: &orderer_git_commit
        type: str
        required: false
        default: v0.0.23
        description:
          - "Git ref or release tag used by the bin build and install branches."
      orderer_source_code_package: &orderer_source_code_package
        type: str
        required: false
        default: cmd/arma
        description:
          - "Go package path that builds the orderer binary."
      orderer_bin_package: &orderer_bin_package
        type: str
        required: false
        default: "{{ orderer_git_hub_url }}/{{ orderer_git_repo }}/{{ orderer_source_code_package }}"
        description:
          - "Go package path used by the install branch."
          - "The default derives from C(orderer_git_hub_url), C(orderer_git_repo), and C(orderer_source_code_package)."
      orderer_bin_name: &orderer_bin_name
        type: str
        required: false
        default: arma
        description:
          - "Binary name used by the bin branches."
      orderer_k8s_resource_name: &orderer_k8s_resource_name
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - "Base name used for the orderer Kubernetes objects, including the optional NodePort Service."
          - "The default derives from C(inventory_hostname)."
      orderer_k8s_wait_timeout: &orderer_k8s_wait_timeout
        type: int
        required: false
        default: 120
        description:
          - "Seconds to wait for the orderer StatefulSet rollout."
      orderer_k8s_fs_group: &orderer_k8s_fs_group
        type: int
        required: false
        default: 10001
        description:
          - "Filesystem group applied to mounted ConfigMap and Secret volumes."
      orderer_k8s_use_node_port: &orderer_k8s_use_node_port
        type: bool
        required: false
        default: false
        description:
          - "Enables the optional NodePort Service for the orderer Kubernetes deployment."
          - "The NodePort Service and Kubernetes ping branch use this toggle."
      orderer_k8s_rpc_node_port: &orderer_k8s_rpc_node_port
        type: int
        required: false
        description:
          - "NodePort used to expose the orderer gRPC service when NodePort exposure is enabled."
          - "The default derives from C(orderer_rpc_port)."
      orderer_k8s_metrics_node_port: &orderer_k8s_metrics_node_port
        type: int
        required: false
        description:
          - "NodePort used to expose the orderer metrics endpoint when NodePort exposure is enabled."
          - "The default derives from C(orderer_metrics_port)."
      orderer_container_config_dir: &orderer_container_config_dir
        type: path
        required: false
        default: /config
        description:
          - "Container path where orderer configuration is mounted."
      orderer_container_data_dir: &orderer_container_data_dir
        type: path
        required: false
        default: /data
        description:
          - "Container path where orderer data is mounted."
      orderer_remote_config_dir: &orderer_remote_config_dir
        type: path
        required: false
        default: "{{ remote_config_dir }}"
        description:
          - "Remote directory where orderer configuration is written."
          - "The default derives from C(remote_config_dir)."
      orderer_remote_data_dir: &orderer_remote_data_dir
        type: path
        required: false
        default: "{{ remote_data_dir }}"
        description:
          - "Remote directory where orderer data is stored."
          - "The default derives from C(remote_data_dir)."
      orderer_config_file: &orderer_config_file
        type: str
        required: false
        default: node_config.yaml
        description:
          - "Rendered orderer configuration filename."
      orderer_genesis_block_file: &orderer_genesis_block_file
        type: str
        required: false
        default: "{{ channel_id }}_block.pb"
        description:
          - "Genesis block filename copied into the config directory."
          - "The default derives from C(channel_id)."
      orderer_crypto_name: &orderer_crypto_name
        type: str
        required: false
        default: "{{ organization.orderer.name | default(inventory_hostname) }}"
        description:
          - "Orderer identity name used to derive crypto artifact paths."
          - "The default derives from C(organization.orderer.name) and falls back to C(inventory_hostname)."
      orderer_config_dir: &orderer_config_dir
        type: path
        required: false
        default: "{{ orderer_remote_config_dir if orderer_use_bin else orderer_container_config_dir }}"
        description:
          - "Configuration path embedded into rendered orderer files."
          - "The default derives from C(orderer_remote_config_dir), C(orderer_use_bin), and C(orderer_container_config_dir)."
      orderer_data_dir: &orderer_data_dir
        type: path
        required: false
        default: "{{ orderer_remote_data_dir if orderer_use_bin else orderer_container_data_dir }}"
        description:
          - "Data path embedded into rendered orderer files."
          - "The default derives from C(orderer_remote_data_dir), C(orderer_use_bin), and C(orderer_container_data_dir)."
      orderer_group: &orderer_group
        type: str
        required: true
        description:
          - "Party identifier written into the orderer configuration."
      orderer_component_type: &orderer_component_type
        type: str
        required: true
        choices:
          - consensus
          - batcher
          - assembler
          - router
        description:
          - "Orderer component to start."
      orderer_shard_id: &orderer_shard_id
        type: int
        required: false
        description:
          - "Batcher shard identifier written only by the batcher template."
      orderer_rpc_port: &orderer_rpc_port
        type: int
        required: true
        description:
          - "gRPC port exposed by the orderer."
      orderer_metrics_port: &orderer_metrics_port
        type: int
        required: true
        description:
          - "Metrics port exposed by the orderer."
      orderer_metrics_port_config_transfer:
        &orderer_metrics_port_config_transfer
        type: int
        required: false
        description:
          - "Metrics port written into the rendered config when enabled."
      orderer_metrics_port_get_metrics: &orderer_metrics_port_get_metrics
        type: int
        required: false
        description:
          - "Metrics port queried by the metrics fetch branch."
      orderer_metrics_log_interval: &orderer_metrics_log_interval
        type: str
        required: false
        description:
          - "Optional metrics logging interval written into the rendered config."
          - "The template falls back to 10s when unset."
      orderer_http_protocol: &orderer_http_protocol
        type: str
        required: false
        default: http
        choices:
          - http
          - https
        description:
          - "Protocol used by the metrics fetch branch."
      orderer_use_bin: &orderer_use_bin
        type: bool
        required: false
        default: false
        description:
          - "Selects the binary deployment branch."
      orderer_use_k8s: &orderer_use_k8s
        type: bool
        required: false
        default: false
        description:
          - "Selects the Kubernetes deployment branch."
      orderer_use_container: &orderer_use_container
        type: bool
        required: false
        default: "{{ (not orderer_use_bin) and (not orderer_use_k8s) }}"
        description:
          - "Selects the container deployment branch."
          - "The default derives from C(orderer_use_bin) and C(orderer_use_k8s)."
      orderer_deployment_mode: &orderer_deployment_mode
        type: str
        required: false
        default: "{%- if orderer_use_bin -%}bin{%- elif orderer_use_k8s -%}k8s{%- else -%}container{%- endif -%}"
        choices:
          - bin
          - container
          - k8s
        description:
          - "Deployment backend selected by the top-level dispatcher."
          - "The default derives from C(orderer_use_bin) and C(orderer_use_k8s)."
      orderer_use_tls: &orderer_use_tls
        type: bool
        required: false
        default: false
        description:
          - "Enables server-side TLS in the rendered config."
      orderer_use_mtls: &orderer_use_mtls
        type: bool
        required: false
        default: false
        description:
          - "Enables client mutual TLS in the rendered config."
      orderer_mtls_clients: &orderer_mtls_clients
        type: list
        elements: str
        required: false
        description:
          - "Client identifiers whose mTLS CA certificates are mounted or transferred."
      orderer_mtls_orgs: &orderer_mtls_orgs
        type: list
        elements: dict
        required: false
        description:
          - "Organization dictionaries whose mTLS CA certificates are mounted or transferred."
      organization: &organization
        type: dict
        required: true
        description:
          - "Organization metadata shared by the orderer crypto and config branches."
      k8s_storage_class: &k8s_storage_class
        type: str
        required: false
        description:
          - "Optional storage class used by the StatefulSet PVC."
      k8s_image_pull_secret: &k8s_image_pull_secret
        type: str
        required: false
        description:
          - "Optional image pull secret used by the StatefulSet."
      k8s_readiness_probe_initial_delay_seconds:
        &k8s_readiness_probe_initial_delay_seconds
        type: int
        required: false
        description:
          - "Optional readiness probe initial delay override."
      k8s_readiness_probe_period_seconds: &k8s_readiness_probe_period_seconds
        type: int
        required: false
        description:
          - "Optional readiness probe period override."
      k8s_readiness_probe_timeout_seconds: &k8s_readiness_probe_timeout_seconds
        type: int
        required: false
        description:
          - "Optional readiness probe timeout override."
      k8s_readiness_probe_failure_threshold:
        &k8s_readiness_probe_failure_threshold
        type: int
        required: false
        description:
          - "Optional readiness probe failure threshold override."
      k8s_liveness_probe_initial_delay_seconds:
        &k8s_liveness_probe_initial_delay_seconds
        type: int
        required: false
        description:
          - "Optional liveness probe initial delay override."
      k8s_liveness_probe_period_seconds: &k8s_liveness_probe_period_seconds
        type: int
        required: false
        description:
          - "Optional liveness probe period override."
      k8s_liveness_probe_timeout_seconds: &k8s_liveness_probe_timeout_seconds
        type: int
        required: false
        description:
          - "Optional liveness probe timeout override."
      k8s_liveness_probe_failure_threshold:
        &k8s_liveness_probe_failure_threshold
        type: int
        required: false
        description:
          - "Optional liveness probe failure threshold override."

  start:
    short_description: Dispatch orderer startup by component and deployment mode
    description:
      - "Selects the orderer component implementation and delegates to the matching deployment-mode start task."
    options:
      orderer_component_type: *orderer_component_type
      orderer_deployment_mode: *orderer_deployment_mode
      orderer_use_bin: *orderer_use_bin
      orderer_use_k8s: *orderer_use_k8s

  stop:
    short_description: Dispatch orderer shutdown by component and deployment mode
    description:
      - "Selects the orderer component implementation and delegates to the matching deployment-mode stop task."
    options:
      orderer_component_type: *orderer_component_type
      orderer_deployment_mode: *orderer_deployment_mode
      orderer_use_bin: *orderer_use_bin
      orderer_use_k8s: *orderer_use_k8s

  teardown:
    short_description: Dispatch orderer teardown by component and deployment mode
    description:
      - "Selects the orderer component implementation and delegates to the matching deployment-mode teardown task."
    options:
      orderer_component_type: *orderer_component_type
      orderer_deployment_mode: *orderer_deployment_mode
      orderer_use_bin: *orderer_use_bin
      orderer_use_k8s: *orderer_use_k8s

  wipe:
    short_description: Remove orderer runtime state, config, and optional binary
    description:
      - "Runs teardown and removes generated configuration, crypto material, and optionally the installed binary."
    options:
      orderer_use_bin: *orderer_use_bin

  fetch_logs:
    short_description: Fetch orderer logs from the active deployment backend
    description:
      - "Delegates log collection to the Kubernetes, container, or binary implementation according to the enabled deployment mode."
    options:
      orderer_use_k8s: *orderer_use_k8s
      orderer_use_container: *orderer_use_container
      orderer_use_bin: *orderer_use_bin

  ping:
    short_description: Check the orderer gRPC port
    description:
      - "Pings the configured orderer gRPC endpoint through the shared utils role, or the Kubernetes NodePort endpoints when enabled."
    options:
      orderer_rpc_port: *orderer_rpc_port
      orderer_use_k8s: *orderer_use_k8s

  get_metrics:
    short_description: Retrieve orderer Prometheus metrics
    description:
      - "Fetches metrics from the orderer monitoring endpoint when metrics are enabled."
    options:
      actual_host: *actual_host
      orderer_http_protocol: *orderer_http_protocol
      orderer_metrics_port: *orderer_metrics_port_get_metrics

  bin/build:
    short_description: Build the orderer binary from source
    description:
      - "Builds the orderer binary through the shared bin role using the configured Git repository and Go package."
    options:
      orderer_bin_name: *orderer_bin_name
      orderer_git_hub_url: *orderer_git_hub_url
      orderer_git_repo: *orderer_git_repo
      orderer_git_commit: *orderer_git_commit
      orderer_source_code_package: *orderer_source_code_package

  bin/install:
    short_description: Install the published orderer binary
    description:
      - "Installs the configured released orderer binary through the shared bin role."
    options:
      orderer_bin_name: *orderer_bin_name
      orderer_bin_package: *orderer_bin_package
      orderer_git_hub_url: *orderer_git_hub_url
      orderer_git_repo: *orderer_git_repo
      orderer_source_code_package: *orderer_source_code_package
      orderer_git_commit: *orderer_git_commit

  bin/transfer:
    short_description: Transfer the orderer binary to the target host
    description:
      - "Copies the built or downloaded orderer binary through the shared bin role."
    options:
      orderer_bin_name: *orderer_bin_name

  bin/rm:
    short_description: Remove the installed orderer binary
    description:
      - "Deletes the orderer binary through the shared bin role."
    options:
      orderer_bin_name: *orderer_bin_name

  bin/start:
    short_description: Start the orderer binary process
    description:
      - "Ensures the data directory exists and starts the orderer binary with the selected component configuration."
    options:
      orderer_bin_name: *orderer_bin_name
      orderer_component_type: *orderer_component_type
      remote_config_dir: *remote_config_dir
      remote_data_dir: *remote_data_dir
      orderer_remote_config_dir: *orderer_remote_config_dir
      orderer_remote_data_dir: *orderer_remote_data_dir
      orderer_config_file: *orderer_config_file
      orderer_rpc_port: *orderer_rpc_port

  bin/stop:
    short_description: Stop the orderer binary process
    description:
      - "Stops the orderer binary through the shared bin role."
    options: {}

  bin/fetch_logs:
    short_description: Fetch logs for the orderer binary process
    description:
      - "Collects binary-process logs through the shared bin role."
    options: {}

  bin/teardown:
    short_description: Remove the orderer binary runtime state
    description:
      - "Stops the orderer binary and removes its persisted data."
    options: {}

  container/start:
    short_description: Start the orderer container
    description:
      - "Ensures the data directory exists and starts the orderer container with the configured ports and volume mounts."
    options:
      orderer_component_type: *orderer_component_type
      orderer_container_name: *orderer_container_name
      orderer_image: *orderer_image
      orderer_registry_endpoint: *orderer_registry_endpoint
      orderer_image_name: *orderer_image_name
      orderer_image_tag: *orderer_image_tag
      remote_config_dir: *remote_config_dir
      remote_data_dir: *remote_data_dir
      orderer_remote_config_dir: *orderer_remote_config_dir
      orderer_remote_data_dir: *orderer_remote_data_dir
      orderer_container_config_dir: *orderer_container_config_dir
      orderer_container_data_dir: *orderer_container_data_dir
      orderer_config_file: *orderer_config_file
      orderer_rpc_port: *orderer_rpc_port
      orderer_metrics_port: *orderer_metrics_port

  container/stop:
    short_description: Stop the orderer container
    description:
      - "Stops the orderer container through the shared container role."
    options:
      orderer_container_name: *orderer_container_name

  container/rm:
    short_description: Remove the orderer container
    description:
      - "Deletes the orderer container through the shared container role."
    options:
      orderer_container_name: *orderer_container_name

  container/fetch_logs:
    short_description: Fetch logs from the orderer container
    description:
      - "Collects logs for the configured orderer container."
    options:
      orderer_container_name: *orderer_container_name

  container/teardown:
    short_description: Remove the orderer container runtime state
    description:
      - "Deletes the orderer container and removes its persisted data."
    options: {}

  data/rm:
    short_description: Remove orderer persisted data
    description:
      - "Deletes the orderer data directory locally or removes the Kubernetes PVC when running in Kubernetes mode."
    options:
      orderer_remote_data_dir: *orderer_remote_data_dir
      remote_data_dir: *remote_data_dir
      orderer_use_k8s: *orderer_use_k8s
      k8s_namespace:
        <<: *k8s_namespace
        required: false

  config/transfer:
    short_description: Render and transfer orderer configuration
    description:
      - "Creates the main orderer configuration file, copies the genesis block, and optionally prepares mTLS and Kubernetes-specific configuration."
    options:
      orderer_component_type: *orderer_component_type
      remote_config_dir: *remote_config_dir
      remote_data_dir: *remote_data_dir
      orderer_remote_config_dir: *orderer_remote_config_dir
      orderer_remote_data_dir: *orderer_remote_data_dir
      orderer_container_config_dir: *orderer_container_config_dir
      orderer_container_data_dir: *orderer_container_data_dir
      orderer_config_dir: *orderer_config_dir
      orderer_data_dir: *orderer_data_dir
      orderer_config_file: *orderer_config_file
      configtxgen_artifacts_dir: *configtxgen_artifacts_dir
      channel_id: *channel_id
      orderer_genesis_block_file: *orderer_genesis_block_file
      orderer_use_bin: *orderer_use_bin
      orderer_use_tls: *orderer_use_tls
      orderer_use_mtls: *orderer_use_mtls
      orderer_use_k8s: *orderer_use_k8s
      orderer_rpc_port: *orderer_rpc_port
      orderer_metrics_port: *orderer_metrics_port_config_transfer
      orderer_metrics_log_interval: *orderer_metrics_log_interval
      orderer_mtls_clients: *orderer_mtls_clients
      orderer_mtls_orgs: *orderer_mtls_orgs
      organization: *organization
      orderer_group: *orderer_group
      orderer_shard_id: *orderer_shard_id

  config/mtls/transfer:
    short_description: Transfer mTLS CA certificates for orderer clients and orgs
    description:
      - "Copies trusted client and organization TLS CA certificates into the orderer mTLS directory structure."
    options:
      remote_config_dir: *remote_config_dir
      fetched_artifacts_dir: *fetched_artifacts_dir
      orderer_remote_config_dir: *orderer_remote_config_dir
      orderer_mtls_clients: *orderer_mtls_clients
      orderer_mtls_orgs: *orderer_mtls_orgs

  config/rm:
    short_description: Remove orderer configuration
    description:
      - "Deletes the orderer configuration directory and optionally removes Kubernetes ConfigMap resources."
    options:
      remote_config_dir: *remote_config_dir
      orderer_remote_config_dir: *orderer_remote_config_dir
      orderer_use_k8s: *orderer_use_k8s

  config/transfer_grafana_dashboard:
    short_description: Copy the orderer Grafana dashboard
    description:
      - "Publishes the bundled Fabric-X Orderer Grafana dashboard through the grafana role."
    options: {}

  crypto/setup:
    short_description: Prepare orderer crypto material
    description:
      - "Validates TLS prerequisites, provisions crypto material through cryptogen or Fabric CA, and optionally creates the Kubernetes Secret."
    options:
      orderer_use_tls: *orderer_use_tls
      orderer_use_mtls: *orderer_use_mtls
      organization: *organization
      orderer_use_k8s: *orderer_use_k8s

  crypto/cryptogen/transfer:
    short_description: Transfer cryptogen-generated orderer crypto material
    description:
      - "Copies cryptogen-generated MSP and TLS artifacts into the remote orderer configuration directory."
    options:
      cryptogen_artifacts_dir: *cryptogen_artifacts_dir
      remote_config_dir: *remote_config_dir
      organization: *organization
      orderer_crypto_name: *orderer_crypto_name
      orderer_remote_config_dir: *orderer_remote_config_dir

  crypto/fabric_ca/enroll:
    short_description: Enroll the orderer with Fabric CA
    description:
      - "Copies the Fabric CA TLS certificate when needed and enrolls both MSP and TLS identities for the orderer."
    options:
      fetched_artifacts_dir: *fetched_artifacts_dir
      actual_host: *actual_host
      remote_config_dir: *remote_config_dir
      organization: *organization
      orderer_remote_config_dir: *orderer_remote_config_dir

  crypto/fetch:
    short_description: Fetch orderer certificates to the control node
    description:
      - "Fetches the orderer sign certificate, TLS certificate, and TLS CA certificate for downstream artifact generation."
    options:
      fetched_artifacts_dir: *fetched_artifacts_dir
      remote_config_dir: *remote_config_dir
      organization: *organization
      orderer_crypto_name: *orderer_crypto_name
      orderer_remote_config_dir: *orderer_remote_config_dir

  crypto/rm:
    short_description: Remove orderer crypto material
    description:
      - "Deletes the orderer MSP and TLS directories and optionally removes the Kubernetes Secret."
    options:
      remote_config_dir: *remote_config_dir
      orderer_remote_config_dir: *orderer_remote_config_dir
      orderer_use_k8s: *orderer_use_k8s

  k8s/start:
    short_description: Create the orderer Kubernetes workload
    description:
      - "Creates the orderer Services and StatefulSet in Kubernetes after ensuring the namespace exists, and optionally creates the NodePort Service."
    options:
      orderer_component_type: *orderer_component_type
      orderer_k8s_resource_name: *orderer_k8s_resource_name
      orderer_k8s_wait_timeout: *orderer_k8s_wait_timeout
      orderer_k8s_use_node_port: *orderer_k8s_use_node_port
      orderer_rpc_port: *orderer_rpc_port
      orderer_metrics_port: *orderer_metrics_port
      orderer_k8s_rpc_node_port: *orderer_k8s_rpc_node_port
      orderer_k8s_metrics_node_port: *orderer_k8s_metrics_node_port
      orderer_k8s_fs_group: *orderer_k8s_fs_group
      orderer_image: *orderer_image
      orderer_registry_endpoint: *orderer_registry_endpoint
      orderer_image_name: *orderer_image_name
      orderer_image_tag: *orderer_image_tag
      orderer_container_config_dir: *orderer_container_config_dir
      orderer_container_data_dir: *orderer_container_data_dir
      orderer_use_mtls: *orderer_use_mtls
      orderer_mtls_clients: *orderer_mtls_clients
      orderer_mtls_orgs: *orderer_mtls_orgs
      organization: *organization
      k8s_namespace:
        <<: *k8s_namespace
        required: true
      k8s_storage_size: *k8s_storage_size
      k8s_storage_class: *k8s_storage_class
      k8s_image_pull_secret: *k8s_image_pull_secret
      k8s_readiness_probe_initial_delay_seconds: *k8s_readiness_probe_initial_delay_seconds
      k8s_readiness_probe_period_seconds: *k8s_readiness_probe_period_seconds
      k8s_readiness_probe_timeout_seconds: *k8s_readiness_probe_timeout_seconds
      k8s_readiness_probe_failure_threshold: *k8s_readiness_probe_failure_threshold
      k8s_liveness_probe_initial_delay_seconds: *k8s_liveness_probe_initial_delay_seconds
      k8s_liveness_probe_period_seconds: *k8s_liveness_probe_period_seconds
      k8s_liveness_probe_timeout_seconds: *k8s_liveness_probe_timeout_seconds
      k8s_liveness_probe_failure_threshold: *k8s_liveness_probe_failure_threshold

  k8s/ping:
    short_description: Check the orderer Kubernetes Service ports
    description:
      - "Checks the Kubernetes NodePort endpoints when NodePort exposure is enabled."
    options:
      orderer_k8s_use_node_port: *orderer_k8s_use_node_port
      orderer_k8s_rpc_node_port: *orderer_k8s_rpc_node_port
      orderer_k8s_metrics_node_port: *orderer_k8s_metrics_node_port

  k8s/rm:
    short_description: Remove the orderer Kubernetes workload
    description:
      - "Deletes the orderer StatefulSet and Services from Kubernetes."
    options:
      orderer_k8s_resource_name: *orderer_k8s_resource_name
      k8s_namespace:
        <<: *k8s_namespace
        required: true

  k8s/teardown:
    short_description: Remove the orderer Kubernetes workload and data
    description:
      - "Deletes the Kubernetes workload and removes persisted orderer data."
    options: {}

  k8s/fetch_logs:
    short_description: Fetch logs from the orderer Kubernetes pod
    description:
      - "Collects logs from pods selected by the orderer Kubernetes app label."
    options:
      orderer_k8s_resource_name: *orderer_k8s_resource_name

  k8s/config/transfer:
    short_description: Create the orderer Kubernetes ConfigMap
    description:
      - "Slurps the genesis block and renders the orderer ConfigMap, including optional mTLS CA bundles."
    options:
      remote_config_dir: *remote_config_dir
      orderer_remote_config_dir: *orderer_remote_config_dir
      orderer_k8s_resource_name: *orderer_k8s_resource_name
      orderer_component_type: *orderer_component_type
      orderer_config_file: *orderer_config_file
      orderer_use_mtls: *orderer_use_mtls
      orderer_mtls_clients: *orderer_mtls_clients
      orderer_mtls_orgs: *orderer_mtls_orgs
      k8s_namespace:
        <<: *k8s_namespace
        required: true

  k8s/config/rm:
    short_description: Remove the orderer Kubernetes ConfigMap
    description:
      - "Deletes the ConfigMap that holds orderer configuration and genesis material."
    options:
      orderer_k8s_resource_name: *orderer_k8s_resource_name
      k8s_namespace:
        <<: *k8s_namespace
        required: true

  k8s/crypto/transfer:
    short_description: Create the orderer Kubernetes Secret
    description:
      - "Resolves the orderer crypto file locations and renders the Kubernetes Secret containing MSP and TLS material."
    options:
      remote_config_dir: *remote_config_dir
      orderer_remote_config_dir: *orderer_remote_config_dir
      organization: *organization
      orderer_crypto_name: *orderer_crypto_name
      orderer_k8s_resource_name: *orderer_k8s_resource_name
      orderer_component_type: *orderer_component_type
      k8s_namespace:
        <<: *k8s_namespace
        required: true

  k8s/crypto/rm:
    short_description: Remove the orderer Kubernetes Secret
    description:
      - "Deletes the Secret that stores orderer MSP and TLS material."
    options:
      orderer_k8s_resource_name: *orderer_k8s_resource_name
      k8s_namespace:
        <<: *k8s_namespace
        required: true

  prometheus/get_scrapers:
    short_description: Build Prometheus scrape targets for orderer hosts
    description:
      - "Groups orderer hosts by component type and exposes Prometheus scrape service definitions for downstream monitoring configuration."
    options:
      orderer_hosts: *orderer_hosts
