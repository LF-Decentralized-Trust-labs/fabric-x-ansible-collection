#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#
---
galaxy_info:
  author: IBM
  description: "Performs a live channel-config update against a running Fabric-X network (v1 scope: changing one party's Assembler endpoint), split across fetch/render/compute, per-party out-of-band signing, and submit/verify/restart stages."
  license: Apache-2.0
  min_ansible_version: "2.17.0"
  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: EL
      versions:
        - all
    - name: MacOSX
      versions:
        - all

dependencies: []
