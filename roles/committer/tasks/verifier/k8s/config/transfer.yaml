#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#

# =========================
# Namespace
# =========================
- name: Ensure Kubernetes namespace exists for Fabric-X Committer verifier
  ansible.builtin.include_role:
    name: hyperledger.fabricx.k8s
    tasks_from: namespace/create

# =========================
# mTLS client/org cert lists
# =========================
- name: Prepare mTLS cert lists for Fabric-X Committer verifier
  ansible.builtin.include_role:
    name: hyperledger.fabricx.committer
    tasks_from: k8s/config/mtls/transfer
  when: committer_use_mtls

# =========================
# Apply ConfigMap
# =========================
- name: Apply Fabric-X Committer verifier ConfigMap
  vars:
    committer_k8s_config_content: "{{ lookup('template', 'config-verifier.yaml.j2') }}"
  kubernetes.core.k8s:
    state: present
    template:
      path: k8s/configmap-verifier.yaml.j2
