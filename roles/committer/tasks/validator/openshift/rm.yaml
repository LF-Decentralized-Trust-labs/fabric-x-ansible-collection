#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#

- name: Delete Hyperledger Fabric-X Committer validator rpc OpenShift Route
  vars:
    openshift_route_name: "{{ committer_k8s_resource_name }}-rpc"
  ansible.builtin.include_role:
    name: hyperledger.fabricx.openshift
    tasks_from: route/rm
  when: committer_openshift_route is defined

- name: Delete Hyperledger Fabric-X Committer validator metrics OpenShift Route
  vars:
    openshift_route_name: "{{ committer_k8s_resource_name }}-metrics"
  ansible.builtin.include_role:
    name: hyperledger.fabricx.openshift
    tasks_from: route/rm
  when: committer_openshift_metrics_route is defined

- name: Remove Hyperledger Fabric-X Committer validator workload using generic k8s flow
  ansible.builtin.include_role:
    name: hyperledger.fabricx.committer
    tasks_from: validator/k8s/rm
