#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#
---
argument_specs:

  ping:
    short_description: Check the committer gRPC endpoint
    description:
      - Validate that the Fabric-X Committer RPC port is reachable.
    options:
      committer_rpc_port:
        type: int
        required: true
        description:
          - gRPC port exposed by the committer component.

  validator/start:
    short_description: Start a validator component
    description:
      - Start a validator in bin, container, or Kubernetes mode.
      - The role derives the deployment mode from `committer_use_bin` and `committer_use_k8s`.
    options:
      committer_component_type:
        type: str
        required: true
        choices: [validator]
        description:
          - Committer component to start. Use `validator` for this entry point.
      committer_rpc_port:
        type: int
        required: true
        description:
          - RPC port exposed by the validator.
      committer_metrics_port:
        type: int
        required: true
        description:
          - Metrics port exposed by the validator.
      remote_config_dir:
        type: path
        required: true
        description:
          - Base remote directory for generated committer configuration files.
      committer_use_bin:
        type: bool
        default: false
        description:
          - Start the validator as a host binary.
      committer_use_k8s:
        type: bool
        default: false
        description:
          - Start the validator on Kubernetes. When true, set `k8s_namespace`.
      committer_bin_name:
        type: str
        default: committer
        description:
          - Binary name to execute in bin mode.
      committer_container_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Container name to use in container mode.
      committer_image:
        type: str
        default: "{{ committer_registry_endpoint }}/{{ committer_image_name }}:{{ committer_image_tag }}"
        description:
          - Container image to run in container or Kubernetes mode.
      committer_k8s_resource_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for services and workloads.
      committer_k8s_wait_timeout:
        type: int
        default: 120
        description:
          - Wait timeout in seconds for the Kubernetes workload to become ready.
      committer_k8s_fs_group:
        type: int
        default: 10001
        description:
          - Filesystem group assigned to the Kubernetes pod security context.
      committer_use_tls:
        type: bool
        default: false
        description:
          - Mount TLS material into the workload when enabled.
      k8s_namespace:
        type: str
        description:
          - Kubernetes namespace used when `committer_use_k8s` is true.
      postgres_db_host:
        type: str
        description:
          - Postgres host inventory name. Needed when validator DB TLS assets are mounted from Postgres.
      yugabyte_cluster_ref_id:
        type: str
        description:
          - Yugabyte cluster identifier. Needed when validator DB TLS assets are mounted from Yugabyte.

  verifier/start:
    short_description: Start a verifier component
    description:
      - Start a verifier in bin, container, or Kubernetes mode.
    options:
      committer_component_type:
        type: str
        required: true
        choices: [verifier]
        description:
          - Committer component to start. Use `verifier` for this entry point.
      committer_rpc_port:
        type: int
        required: true
        description:
          - RPC port exposed by the verifier.
      committer_metrics_port:
        type: int
        required: true
        description:
          - Metrics port exposed by the verifier.
      remote_config_dir:
        type: path
        required: true
        description:
          - Base remote directory for generated committer configuration files.
      committer_use_bin:
        type: bool
        default: false
        description:
          - Start the verifier as a host binary.
      committer_use_k8s:
        type: bool
        default: false
        description:
          - Start the verifier on Kubernetes. When true, set `k8s_namespace`.
      committer_bin_name:
        type: str
        default: committer
        description:
          - Binary name to execute in bin mode.
      committer_container_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Container name to use in container mode.
      committer_image:
        type: str
        default: "{{ committer_registry_endpoint }}/{{ committer_image_name }}:{{ committer_image_tag }}"
        description:
          - Container image to run in container or Kubernetes mode.
      committer_k8s_resource_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for services and workloads.
      committer_k8s_wait_timeout:
        type: int
        default: 120
        description:
          - Wait timeout in seconds for the Kubernetes workload to become ready.
      committer_k8s_fs_group:
        type: int
        default: 10001
        description:
          - Filesystem group assigned to the Kubernetes pod security context.
      committer_use_tls:
        type: bool
        default: false
        description:
          - Mount TLS material into the workload when enabled.
      k8s_namespace:
        type: str
        description:
          - Kubernetes namespace used when `committer_use_k8s` is true.

  coordinator/start:
    short_description: Start a coordinator component
    description:
      - Start a coordinator in bin, container, or Kubernetes mode.
      - Coordinators connect to validator and verifier hosts that must already be configured.
    options:
      committer_component_type:
        type: str
        required: true
        choices: [coordinator]
        description:
          - Committer component to start. Use `coordinator` for this entry point.
      committer_rpc_port:
        type: int
        required: true
        description:
          - RPC port exposed by the coordinator.
      committer_metrics_port:
        type: int
        required: true
        description:
          - Metrics port exposed by the coordinator.
      remote_config_dir:
        type: path
        required: true
        description:
          - Base remote directory for generated committer configuration files.
      committer_use_bin:
        type: bool
        default: false
        description:
          - Start the coordinator as a host binary.
      committer_use_k8s:
        type: bool
        default: false
        description:
          - Start the coordinator on Kubernetes. When true, set `k8s_namespace`.
      committer_bin_name:
        type: str
        default: committer
        description:
          - Binary name to execute in bin mode.
      committer_container_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Container name to use in container mode.
      committer_image:
        type: str
        default: "{{ committer_registry_endpoint }}/{{ committer_image_name }}:{{ committer_image_tag }}"
        description:
          - Container image to run in container or Kubernetes mode.
      committer_k8s_resource_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for services and workloads.
      committer_k8s_wait_timeout:
        type: int
        default: 120
        description:
          - Wait timeout in seconds for the Kubernetes workload to become ready.
      committer_k8s_fs_group:
        type: int
        default: 10001
        description:
          - Filesystem group assigned to the Kubernetes pod security context.
      committer_use_tls:
        type: bool
        default: false
        description:
          - Mount TLS material into the workload when enabled.
      k8s_namespace:
        type: str
        description:
          - Kubernetes namespace used when `committer_use_k8s` is true.
      committer_validators:
        type: list
        description:
          - Inventory hosts for validator components. Needed when starting the coordinator on Kubernetes.
      committer_verifiers:
        type: list
        description:
          - Inventory hosts for verifier components. Needed when starting the coordinator on Kubernetes.

  sidecar/start:
    short_description: Start a sidecar component
    description:
      - Start a sidecar in bin, container, or Kubernetes mode.
      - Sidecars require persistent data storage and connect to coordinator and orderer hosts.
    options:
      committer_component_type:
        type: str
        required: true
        choices: [sidecar]
        description:
          - Committer component to start. Use `sidecar` for this entry point.
      committer_rpc_port:
        type: int
        required: true
        description:
          - RPC port exposed by the sidecar.
      committer_metrics_port:
        type: int
        required: true
        description:
          - Metrics port exposed by the sidecar.
      remote_config_dir:
        type: path
        required: true
        description:
          - Base remote directory for generated committer configuration files.
      remote_data_dir:
        type: path
        required: true
        description:
          - Base remote directory for persistent committer data.
      committer_use_bin:
        type: bool
        default: false
        description:
          - Start the sidecar as a host binary.
      committer_use_k8s:
        type: bool
        default: false
        description:
          - Start the sidecar on Kubernetes. When true, set `k8s_namespace` and `k8s_storage_size`.
      committer_bin_name:
        type: str
        default: committer
        description:
          - Binary name to execute in bin mode.
      committer_container_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Container name to use in container mode.
      committer_image:
        type: str
        default: "{{ committer_registry_endpoint }}/{{ committer_image_name }}:{{ committer_image_tag }}"
        description:
          - Container image to run in container or Kubernetes mode.
      committer_k8s_resource_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for services and workloads.
      committer_k8s_wait_timeout:
        type: int
        default: 120
        description:
          - Wait timeout in seconds for the Kubernetes workload to become ready.
      committer_k8s_fs_group:
        type: int
        default: 10001
        description:
          - Filesystem group assigned to the Kubernetes pod security context.
      committer_use_tls:
        type: bool
        default: false
        description:
          - Mount TLS material into the workload when enabled.
      k8s_namespace:
        type: str
        description:
          - Kubernetes namespace used when `committer_use_k8s` is true.
      k8s_storage_size:
        type: str
        description:
          - Persistent volume size requested by the sidecar StatefulSet.
      committer_coordinator:
        type: str
        description:
          - Inventory host name of the coordinator used by the sidecar.
      orderer_assemblers:
        type: list
        description:
          - Inventory host names of orderer assembler nodes used by the sidecar.

  query_service/start:
    short_description: Start a query-service component
    description:
      - Start a query-service in bin, container, or Kubernetes mode.
    options:
      committer_component_type:
        type: str
        required: true
        choices: [query-service]
        description:
          - Committer component to start. Use `query-service` for this entry point.
      committer_rpc_port:
        type: int
        required: true
        description:
          - RPC port exposed by the query-service.
      committer_metrics_port:
        type: int
        required: true
        description:
          - Metrics port exposed by the query-service.
      remote_config_dir:
        type: path
        required: true
        description:
          - Base remote directory for generated committer configuration files.
      committer_use_bin:
        type: bool
        default: false
        description:
          - Start the query-service as a host binary.
      committer_use_k8s:
        type: bool
        default: false
        description:
          - Start the query-service on Kubernetes. When true, set `k8s_namespace`.
      committer_bin_name:
        type: str
        default: committer
        description:
          - Binary name to execute in bin mode.
      committer_container_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Container name to use in container mode.
      committer_image:
        type: str
        default: "{{ committer_registry_endpoint }}/{{ committer_image_name }}:{{ committer_image_tag }}"
        description:
          - Container image to run in container or Kubernetes mode.
      committer_k8s_resource_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for services and workloads.
      committer_k8s_wait_timeout:
        type: int
        default: 120
        description:
          - Wait timeout in seconds for the Kubernetes workload to become ready.
      committer_k8s_fs_group:
        type: int
        default: 10001
        description:
          - Filesystem group assigned to the Kubernetes pod security context.
      committer_use_tls:
        type: bool
        default: false
        description:
          - Mount TLS material into the workload when enabled.
      k8s_namespace:
        type: str
        description:
          - Kubernetes namespace used when `committer_use_k8s` is true.
      postgres_db_host:
        type: str
        description:
          - Postgres host inventory name. Needed when query-service DB TLS assets are mounted from Postgres.
      yugabyte_cluster_ref_id:
        type: str
        description:
          - Yugabyte cluster identifier. Needed when query-service DB TLS assets are mounted from Yugabyte.

  stop:
    short_description: Stop a committer process
    description:
      - Stop the selected component in bin or container mode.
      - Kubernetes teardown is handled through the teardown entry points instead.
    options:
      committer_component_type:
        type: str
        required: true
        choices: [validator, verifier, coordinator, sidecar, query-service]
        description:
          - Committer component to stop.
      committer_use_bin:
        type: bool
        default: false
        description:
          - Use bin mode for the stop operation.
      committer_use_k8s:
        type: bool
        default: false
        description:
          - Kubernetes mode is ignored by this entry point.
      committer_container_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Container name to stop in container mode.

  teardown:
    short_description: Teardown a selected component
    description:
      - Remove the selected component according to its deployment mode.
      - Sidecar teardown also removes sidecar data.
    options:
      committer_component_type:
        type: str
        required: true
        choices: [validator, verifier, coordinator, sidecar, query-service]
        description:
          - Committer component to tear down.
      committer_use_bin:
        type: bool
        default: false
        description:
          - Teardown the host binary process and assets for the selected component.
      committer_use_k8s:
        type: bool
        default: false
        description:
          - Remove Kubernetes resources for the selected component.
      committer_container_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Container name to remove in container mode.
      remote_data_dir:
        type: path
        description:
          - Base remote directory for sidecar data. Needed when tearing down a sidecar.
      committer_k8s_resource_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for services and workloads.
      k8s_namespace:
        type: str
        description:
          - Kubernetes namespace used when `committer_use_k8s` is true.

  query_service/teardown:
    short_description: Teardown the query-service
    description:
      - Remove the query-service according to its deployment mode.
    options:
      committer_use_bin:
        type: bool
        default: false
        description:
          - Stop the query-service binary.
      committer_use_k8s:
        type: bool
        default: false
        description:
          - Remove Kubernetes resources for the query-service.
      committer_container_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Container name to remove in container mode.
      committer_k8s_resource_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for services and workloads.
      k8s_namespace:
        type: str
        description:
          - Kubernetes namespace used when `committer_use_k8s` is true.

  sidecar/teardown:
    short_description: Teardown the sidecar
    description:
      - Remove the sidecar according to its deployment mode and delete sidecar data.
    options:
      committer_use_bin:
        type: bool
        default: false
        description:
          - Stop the sidecar binary.
      committer_use_k8s:
        type: bool
        default: false
        description:
          - Remove Kubernetes resources for the sidecar.
      committer_container_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Container name to remove in container mode.
      remote_data_dir:
        type: path
        required: true
        description:
          - Base remote directory for sidecar data.
      committer_k8s_resource_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for services and workloads.
      k8s_namespace:
        type: str
        description:
          - Kubernetes namespace used when `committer_use_k8s` is true.

  coordinator/teardown:
    short_description: Teardown the coordinator
    description:
      - Remove the coordinator according to its deployment mode.
    options:
      committer_use_bin:
        type: bool
        default: false
        description:
          - Stop the coordinator binary.
      committer_use_k8s:
        type: bool
        default: false
        description:
          - Remove Kubernetes resources for the coordinator.
      committer_container_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Container name to remove in container mode.
      committer_k8s_resource_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for services and workloads.
      k8s_namespace:
        type: str
        description:
          - Kubernetes namespace used when `committer_use_k8s` is true.

  wipe:
    short_description: Remove all committer artifacts
    description:
      - Tear down the selected component and remove binary, config, and crypto assets.
    options:
      committer_component_type:
        type: str
        required: true
        choices: [validator, verifier, coordinator, sidecar, query-service]
        description:
          - Committer component whose artifacts will be removed.
      committer_use_bin:
        type: bool
        default: false
        description:
          - Remove the binary in bin mode.
      committer_use_k8s:
        type: bool
        default: false
        description:
          - Remove Kubernetes config and secret resources when enabled.
      committer_container_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Container name to remove in container mode.
      remote_config_dir:
        type: path
        required: true
        description:
          - Base remote directory for generated committer configuration files.
      remote_data_dir:
        type: path
        description:
          - Base remote directory for sidecar data. Needed when wiping a sidecar.
      committer_use_tls:
        type: bool
        default: false
        description:
          - Remove TLS material when enabled.
      committer_k8s_resource_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for services and workloads.
      k8s_namespace:
        type: str
        description:
          - Kubernetes namespace used when `committer_use_k8s` is true.

  fetch_logs:
    short_description: Collect committer logs
    description:
      - Fetch logs from the selected deployment mode.
    options:
      committer_use_bin:
        type: bool
        default: false
        description:
          - Fetch logs from the host binary process.
      committer_use_k8s:
        type: bool
        default: false
        description:
          - Fetch pod logs from Kubernetes.
      committer_container_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Container name to inspect in container mode.
      committer_k8s_resource_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used to select pods.

  get_metrics:
    short_description: Retrieve Prometheus metrics
    description:
      - Query the component metrics endpoint and print the response body.
      - This task is a no-op when `committer_metrics_port` is not defined.
    options:
      actual_host:
        type: str
        required: true
        description:
          - Hostname or IP address used to reach the metrics endpoint.
      committer_metrics_port:
        type: int
        description:
          - Metrics port to query.
      committer_use_tls:
        type: bool
        default: false
        description:
          - Use HTTPS when enabled.

  start:
    short_description: Start a committer component by type
    description:
      - Dispatch startup to the selected committer component.
    options:
      committer_component_type:
        type: str
        required: true
        choices: [validator, verifier, coordinator, sidecar, query-service]
        description:
          - Committer component to start.

  crypto/setup:
    short_description: Prepare crypto material
    description:
      - Transfer cryptogen artifacts or enroll with Fabric CA for the selected component.
      - When `committer_use_k8s` is true, also create the Kubernetes secret for the component.
    options:
      organization:
        type: dict
        required: true
        description:
          - Organization definition for the committer host. It must describe a peer organization.
      committer_component_type:
        type: str
        required: true
        choices: [validator, verifier, coordinator, sidecar, query-service]
        description:
          - Committer component whose crypto material will be prepared.
      remote_config_dir:
        type: path
        required: true
        description:
          - Base remote directory where crypto material is written.
      fetched_artifacts_dir:
        type: path
        required: true
        description:
          - Control-node directory used to read or write fetched crypto artifacts.
      cryptogen_artifacts_dir:
        type: path
        description:
          - Control-node directory containing cryptogen output. Needed when `organization.fabric_ca_host` is not set.
      committer_use_tls:
        type: bool
        default: false
        description:
          - Generate or transfer TLS material when enabled.
      committer_use_k8s:
        type: bool
        default: false
        description:
          - Create the Kubernetes secret when enabled.
      committer_k8s_resource_name:
        type: str
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for the generated secret.
      k8s_namespace:
        type: str
        description:
          - Kubernetes namespace used when `committer_use_k8s` is true.

  crypto/fetch:
    short_description: Fetch TLS certificates
    description:
      - Fetch the committer TLS CA certificate and server certificate to the control node.
      - This task runs only when `committer_use_tls` is true.
    options:
      remote_config_dir:
        type: path
        required: true
        description:
          - Base remote directory where committer TLS files are stored.
      fetched_artifacts_dir:
        type: path
        required: true
        description:
          - Control-node directory where fetched TLS files are written.
      committer_use_tls:
        type: bool
        default: false
        description:
          - Fetch TLS files only when enabled.

  bin/install:
    short_description: Install the committer binary
    description:
      - Install the committer binary through the shared `bin` role Go installer entry point.
    options:
      committer_bin_name:
        type: str
        default: committer
        description:
          - Binary name to install.
      committer_bin_package:
        type: str
        default: "{{ committer_git_hub_url }}/{{ committer_git_repo }}/{{ committer_source_code_package }}"
        description:
          - Go package path to install.
      committer_git_commit:
        type: str
        default: v0.1.9
        description:
          - Git ref used for installation.

  bin/build:
    short_description: Build the committer binary
    description:
      - Build the committer binary through the shared `bin` role Go build entry point.
    options:
      committer_bin_name:
        type: str
        default: committer
        description:
          - Binary name produced by the build.
      committer_git_hub_url:
        type: str
        default: github.com
        description:
          - GitHub host used to fetch the source repository.
      committer_git_repo:
        type: str
        default: hyperledger/fabric-x-committer
        description:
          - Git repository that contains the committer source.
      committer_git_commit:
        type: str
        default: v0.1.9
        description:
          - Git ref used for the build.
      committer_source_code_package:
        type: str
        default: cmd/committer
        description:
          - Go package path used as the build target.

  bin/start:
    short_description: Start a committer binary by component type
    description:
      - Dispatch binary startup to the selected committer component.
    options:
      committer_component_type:
        type: str
        required: true
        choices: [validator, verifier, coordinator, sidecar]
        description:
          - Committer component to start in binary mode.

  bin/stop:
    short_description: Stop a committer binary
    description:
      - Stop the running committer binary process through the shared `bin` role.
    options: {}

  bin/rm:
    short_description: Remove the committer binary
    description:
      - Remove the installed committer binary from the target host.
    options:
      committer_bin_name:
        type: str
        required: false
        default: committer
        description:
          - Binary name to remove.

  bin/fetch_logs:
    short_description: Fetch committer binary logs
    description:
      - Collect logs generated by the committer binary through the shared `bin` role.
    options: {}

  bin/transfer:
    short_description: Transfer the committer binary
    description:
      - Copy the built committer binary to the target host.
    options:
      committer_bin_name:
        type: str
        required: false
        default: committer
        description:
          - Binary name to transfer.

  container/start:
    short_description: Start a committer container by component type
    description:
      - Dispatch container startup to the selected committer component.
    options:
      committer_component_type:
        type: str
        required: true
        choices: [validator, verifier, coordinator, sidecar]
        description:
          - Committer component to start in container mode.

  validator/container/start:
    short_description: Start the validator container
    description:
      - Run the validator container with its generated configuration directory mounted read-only.
    options:
      committer_container_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Container name to start.
      committer_image:
        type: str
        required: false
        default: "{{ committer_registry_endpoint }}/{{ committer_image_name }}:{{ committer_image_tag }}"
        description:
          - Container image used for the validator.
      committer_container_config_dir:
        type: path
        required: false
        default: /hlf/config
        description:
          - Config directory path inside the container.
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory mounted into the container.
      committer_config_file:
        type: str
        required: false
        default: config.yaml
        description:
          - Validator config file name.
      committer_rpc_port:
        type: int
        required: true
        description:
          - RPC port exposed by the validator container.
      committer_metrics_port:
        type: int
        required: true
        description:
          - Metrics port exposed by the validator container.

  verifier/container/start:
    short_description: Start the verifier container
    description:
      - Run the verifier container with its generated configuration directory mounted read-only.
    options:
      committer_container_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Container name to start.
      committer_image:
        type: str
        required: false
        default: "{{ committer_registry_endpoint }}/{{ committer_image_name }}:{{ committer_image_tag }}"
        description:
          - Container image used for the verifier.
      committer_container_config_dir:
        type: path
        required: false
        default: /hlf/config
        description:
          - Config directory path inside the container.
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory mounted into the container.
      committer_config_file:
        type: str
        required: false
        default: config.yaml
        description:
          - Verifier config file name.
      committer_rpc_port:
        type: int
        required: true
        description:
          - RPC port exposed by the verifier container.
      committer_metrics_port:
        type: int
        required: true
        description:
          - Metrics port exposed by the verifier container.

  coordinator/container/start:
    short_description: Start the coordinator container
    description:
      - Run the coordinator container with its generated configuration directory mounted read-only.
    options:
      committer_container_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Container name to start.
      committer_image:
        type: str
        required: false
        default: "{{ committer_registry_endpoint }}/{{ committer_image_name }}:{{ committer_image_tag }}"
        description:
          - Container image used for the coordinator.
      committer_container_config_dir:
        type: path
        required: false
        default: /hlf/config
        description:
          - Config directory path inside the container.
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory mounted into the container.
      committer_config_file:
        type: str
        required: false
        default: config.yaml
        description:
          - Coordinator config file name.
      committer_rpc_port:
        type: int
        required: true
        description:
          - RPC port exposed by the coordinator container.
      committer_metrics_port:
        type: int
        required: true
        description:
          - Metrics port exposed by the coordinator container.

  sidecar/container/start:
    short_description: Start the sidecar container
    description:
      - Ensure the sidecar data directory exists and run the sidecar container with config and data volumes mounted.
    options:
      committer_container_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Container name to start.
      committer_image:
        type: str
        required: false
        default: "{{ committer_registry_endpoint }}/{{ committer_image_name }}:{{ committer_image_tag }}"
        description:
          - Container image used for the sidecar.
      committer_container_config_dir:
        type: path
        required: false
        default: /hlf/config
        description:
          - Config directory path inside the container.
      committer_container_data_dir:
        type: path
        required: false
        default: /hlf/data
        description:
          - Data directory path inside the container.
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory mounted into the container.
      committer_remote_data_dir:
        type: path
        required: true
        description:
          - Remote data directory mounted into the container.
      committer_config_file:
        type: str
        required: false
        default: config.yaml
        description:
          - Sidecar config file name.
      committer_rpc_port:
        type: int
        required: true
        description:
          - RPC port exposed by the sidecar container.
      committer_metrics_port:
        type: int
        required: true
        description:
          - Metrics port exposed by the sidecar container.

  query_service/container/start:
    short_description: Start the query-service container
    description:
      - Run the query-service container with its generated configuration directory mounted read-only.
    options:
      committer_container_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Container name to start.
      committer_image:
        type: str
        required: false
        default: "{{ committer_registry_endpoint }}/{{ committer_image_name }}:{{ committer_image_tag }}"
        description:
          - Container image used for the query-service.
      committer_container_config_dir:
        type: path
        required: false
        default: /hlf/config
        description:
          - Config directory path inside the container.
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory mounted into the container.
      committer_config_file:
        type: str
        required: false
        default: config.yaml
        description:
          - Query-service config file name.
      committer_rpc_port:
        type: int
        required: true
        description:
          - RPC port exposed by the query-service container.
      committer_metrics_port:
        type: int
        required: true
        description:
          - Metrics port exposed by the query-service container.

  container/stop:
    short_description: Stop a committer container
    description:
      - Stop the committer container through the shared `container` role.
    options:
      committer_container_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Container name to stop.

  container/rm:
    short_description: Remove a committer container
    description:
      - Remove the committer container through the shared `container` role.
    options:
      committer_container_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Container name to remove.

  container/fetch_logs:
    short_description: Fetch committer container logs
    description:
      - Collect logs from the committer container through the shared `container` role.
    options:
      committer_container_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Container name whose logs are fetched.

  config/transfer:
    short_description: Generate config by component type
    description:
      - Dispatch configuration generation to the selected committer component.
    options:
      committer_component_type:
        type: str
        required: true
        choices: [validator, verifier, coordinator, sidecar, query-service]
        description:
          - Committer component whose configuration is generated.

  config/rm:
    short_description: Remove committer configuration
    description:
      - Remove the component config directory and the Kubernetes ConfigMap when enabled.
    options:
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory to remove.
      committer_use_k8s:
        type: bool
        required: false
        default: false
        description:
          - Remove the Kubernetes ConfigMap when enabled.

  config/transfer_grafana_dashboard:
    short_description: Transfer the committer Grafana dashboard
    description:
      - Publish the committer Grafana dashboard through the shared Grafana helper flow.
    options: {}

  config/db/transfer:
    short_description: Transfer DB config by backend type
    description:
      - Dispatch database configuration generation to the selected backend.
    options:
      postgres_db_host:
        type: str
        description:
          - Postgres host inventory name used when the component relies on PostgreSQL.
      yugabyte_cluster_ref_id:
        type: str
        description:
          - Yugabyte cluster identifier used when the component relies on YugabyteDB.

  config/db/postgres/transfer:
    short_description: Transfer PostgreSQL DB config
    description:
      - Generate the PostgreSQL connection settings consumed by the committer component.
    options:
      postgres_db_host:
        type: str
        required: true
        description:
          - Postgres host inventory name used to resolve DB connection settings.
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory where DB settings are written.

  config/db/yugabyte/transfer:
    short_description: Transfer Yugabyte DB config
    description:
      - Generate the Yugabyte connection settings consumed by the committer component.
    options:
      yugabyte_cluster_ref_id:
        type: str
        required: true
        description:
          - Yugabyte cluster identifier used to resolve DB connection settings.
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory where DB settings are written.

  config/mtls/transfer:
    short_description: Transfer committer mTLS certificates
    description:
      - Copy mTLS certificates for the committer service-to-service connections.
    options:
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory where mTLS certificates are written.
      fetched_artifacts_dir:
        type: path
        required: true
        description:
          - Control-node directory containing fetched TLS artifacts.

  config/mtls/monitoring/transfer:
    short_description: Transfer monitoring mTLS certificates
    description:
      - Copy monitoring mTLS certificates for Prometheus scraping.
    options:
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory where monitoring mTLS certificates are written.
      fetched_artifacts_dir:
        type: path
        required: true
        description:
          - Control-node directory containing fetched monitoring TLS artifacts.

  crypto/rm:
    short_description: Remove committer crypto material
    description:
      - Remove local TLS assets and the Kubernetes Secret when enabled.
    options:
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory that contains the TLS files.
      committer_use_tls:
        type: bool
        required: false
        default: false
        description:
          - Remove local TLS assets when enabled.
      committer_use_k8s:
        type: bool
        required: false
        default: false
        description:
          - Remove the Kubernetes Secret when enabled.

  crypto/cryptogen/transfer:
    short_description: Transfer committer crypto from cryptogen
    description:
      - Copy cryptogen-generated TLS assets for the selected committer component.
    options:
      organization:
        type: dict
        required: true
        description:
          - Fabric organization definition used to resolve the component identity.
      fetched_artifacts_dir:
        type: path
        required: true
        description:
          - Control-node directory containing the generated crypto artifacts.
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory where the TLS files are written.

  crypto/fabric_ca/enroll:
    short_description: Enroll committer crypto with Fabric CA
    description:
      - Enroll the selected committer component against its Fabric CA and write the resulting TLS assets.
    options:
      organization:
        type: dict
        required: true
        description:
          - Fabric organization definition used to resolve the Fabric CA enrollment inputs.
      committer_component_type:
        type: str
        required: true
        choices: [validator, verifier, coordinator, sidecar, query-service]
        description:
          - Committer component whose identity is enrolled.
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory where the enrolled TLS files are written.

  data/rm:
    short_description: Remove sidecar data
    description:
      - Remove the sidecar data directory and sidecar PVC when Kubernetes mode is enabled.
    options:
      committer_remote_data_dir:
        type: path
        required: true
        description:
          - Remote data directory to remove.
      committer_k8s_resource_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used to derive the PVC name.
      committer_use_k8s:
        type: bool
        required: false
        default: false
        description:
          - Remove the sidecar PVC when enabled.
      k8s_namespace:
        type: str
        description:
          - Kubernetes namespace that contains the sidecar PVC.

  k8s/config/rm:
    short_description: Remove the committer ConfigMap
    description:
      - Delete the committer Kubernetes ConfigMap.
    options:
      committer_k8s_resource_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used to derive the ConfigMap name.
      k8s_namespace:
        type: str
        required: true
        description:
          - Kubernetes namespace that contains the ConfigMap.

  k8s/crypto/rm:
    short_description: Remove the committer Secret
    description:
      - Delete the committer Kubernetes Secret.
    options:
      committer_k8s_resource_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used to derive the Secret name.
      k8s_namespace:
        type: str
        required: true
        description:
          - Kubernetes namespace that contains the Secret.

  k8s/crypto/transfer:
    short_description: Create the committer Secret
    description:
      - Create the committer Kubernetes Secret from the generated TLS materials.
    options:
      committer_k8s_resource_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used to derive the Secret name.
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory that contains the TLS files copied into the Secret.
      k8s_namespace:
        type: str
        required: true
        description:
          - Kubernetes namespace where the Secret is created.

  k8s/fetch_logs:
    short_description: Fetch committer pod logs
    description:
      - Collect logs from committer pods through the shared Kubernetes helper role.
    options:
      committer_k8s_resource_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used to select pods.

  prometheus/get_scrapers:
    short_description: Build Prometheus scrape targets for committer
    description:
      - Construct the Prometheus scrape service definitions for all deployed committer component types.
    options:
      committer_hosts:
        type: list
        required: true
        description:
          - Inventory hosts devoted to committer components.
      fetched_artifacts_dir:
        type: path
        required: true
        description:
          - Control-node directory that contains fetched TLS artifacts used by scrape jobs.

  validator/bin/start:
    short_description: Start the validator binary
    description:
      - Run the validator binary with its generated configuration file.
    options:
      committer_bin_name:
        type: str
        required: false
        default: committer
        description:
          - Binary name to execute.
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory that contains the validator config file.
      committer_config_file:
        type: str
        required: false
        default: config.yaml
        description:
          - Validator config file name.
      committer_rpc_port:
        type: int
        required: true
        description:
          - RPC port used to detect readiness.

  verifier/bin/start:
    short_description: Start the verifier binary
    description:
      - Run the verifier binary with its generated configuration file.
    options:
      committer_bin_name:
        type: str
        required: false
        default: committer
        description:
          - Binary name to execute.
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory that contains the verifier config file.
      committer_config_file:
        type: str
        required: false
        default: config.yaml
        description:
          - Verifier config file name.
      committer_rpc_port:
        type: int
        required: true
        description:
          - RPC port used to detect readiness.

  coordinator/bin/start:
    short_description: Start the coordinator binary
    description:
      - Run the coordinator binary with its generated configuration file.
    options:
      committer_bin_name:
        type: str
        required: false
        default: committer
        description:
          - Binary name to execute.
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory that contains the coordinator config file.
      committer_config_file:
        type: str
        required: false
        default: config.yaml
        description:
          - Coordinator config file name.
      committer_rpc_port:
        type: int
        required: true
        description:
          - RPC port used to detect readiness.

  sidecar/bin/start:
    short_description: Start the sidecar binary
    description:
      - Ensure the sidecar data directory exists and run the sidecar binary.
    options:
      committer_bin_name:
        type: str
        required: false
        default: committer
        description:
          - Binary name to execute.
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory that contains the sidecar config file.
      committer_remote_data_dir:
        type: path
        required: true
        description:
          - Remote data directory created for the sidecar.
      committer_config_file:
        type: str
        required: false
        default: config.yaml
        description:
          - Sidecar config file name.
      committer_rpc_port:
        type: int
        required: true
        description:
          - RPC port used to detect readiness.

  query_service/bin/start:
    short_description: Start the query-service binary
    description:
      - Run the query-service binary with its generated configuration file.
    options:
      committer_bin_name:
        type: str
        required: false
        default: committer
        description:
          - Binary name to execute.
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory that contains the query-service config file.
      committer_config_file:
        type: str
        required: false
        default: config.yaml
        description:
          - Query-service config file name.
      committer_rpc_port:
        type: int
        required: true
        description:
          - RPC port used to detect readiness.

  validator/config/transfer:
    short_description: Generate validator config
    description:
      - Render validator configuration, DB settings, mTLS assets, and optional Kubernetes ConfigMap.
    options:
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory where validator files are written.
      committer_config_file:
        type: str
        required: false
        default: config.yaml
        description:
          - Validator config file name.
      postgres_db_host:
        type: str
        description:
          - Postgres host inventory name used for validator persistence.
      yugabyte_cluster_ref_id:
        type: str
        description:
          - Yugabyte cluster identifier used for validator persistence.
      committer_use_mtls:
        type: bool
        required: false
        default: false
        description:
          - Transfer service mTLS certificates when enabled.
      committer_monitoring_use_mtls:
        type: bool
        required: false
        default: false
        description:
          - Transfer monitoring mTLS certificates when enabled.
      committer_use_k8s:
        type: bool
        required: false
        default: false
        description:
          - Generate the validator Kubernetes ConfigMap when enabled.

  verifier/config/transfer:
    short_description: Generate verifier config
    description:
      - Render verifier configuration, mTLS assets, and optional Kubernetes ConfigMap.
    options:
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory where verifier files are written.
      committer_config_file:
        type: str
        required: false
        default: config.yaml
        description:
          - Verifier config file name.
      committer_use_mtls:
        type: bool
        required: false
        default: false
        description:
          - Transfer service mTLS certificates when enabled.
      committer_monitoring_use_mtls:
        type: bool
        required: false
        default: false
        description:
          - Transfer monitoring mTLS certificates when enabled.
      committer_use_k8s:
        type: bool
        required: false
        default: false
        description:
          - Generate the verifier Kubernetes ConfigMap when enabled.

  coordinator/config/transfer:
    short_description: Generate coordinator config
    description:
      - Render coordinator configuration, validator and verifier CA bundles, and optional Kubernetes ConfigMap.
    options:
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory where coordinator files are written.
      committer_config_file:
        type: str
        required: false
        default: config.yaml
        description:
          - Coordinator config file name.
      committer_validators:
        type: list
        required: true
        description:
          - Inventory hosts devoted to validator components.
      committer_verifiers:
        type: list
        required: true
        description:
          - Inventory hosts devoted to verifier components.
      fetched_artifacts_dir:
        type: path
        required: true
        description:
          - Control-node directory containing fetched TLS artifacts.
      committer_use_k8s:
        type: bool
        required: false
        default: false
        description:
          - Generate the coordinator Kubernetes ConfigMap when enabled.

  sidecar/config/transfer:
    short_description: Generate sidecar config
    description:
      - Render sidecar configuration, upstream TLS bundles, and optional Kubernetes ConfigMap.
    options:
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory where sidecar files are written.
      committer_config_file:
        type: str
        required: false
        default: config.yaml
        description:
          - Sidecar config file name.
      orderer_assemblers:
        type: list
        required: true
        description:
          - Inventory hosts devoted to orderer assembler components.
      fetched_artifacts_dir:
        type: path
        required: true
        description:
          - Control-node directory containing fetched TLS artifacts.
      committer_use_k8s:
        type: bool
        required: false
        default: false
        description:
          - Generate the sidecar Kubernetes ConfigMap when enabled.

  query_service/config/transfer:
    short_description: Generate query-service config
    description:
      - Render query-service configuration, DB settings, mTLS assets, and optional Kubernetes ConfigMap.
    options:
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory where query-service files are written.
      committer_config_file:
        type: str
        required: false
        default: config.yaml
        description:
          - Query-service config file name.
      postgres_db_host:
        type: str
        description:
          - Postgres host inventory name used for query-service persistence.
      yugabyte_cluster_ref_id:
        type: str
        description:
          - Yugabyte cluster identifier used for query-service persistence.
      committer_use_mtls:
        type: bool
        required: false
        default: false
        description:
          - Transfer service mTLS certificates when enabled.
      committer_monitoring_use_mtls:
        type: bool
        required: false
        default: false
        description:
          - Transfer monitoring mTLS certificates when enabled.
      committer_use_k8s:
        type: bool
        required: false
        default: false
        description:
          - Generate the query-service Kubernetes ConfigMap when enabled.

  validator/k8s/start:
    short_description: Start the validator on Kubernetes
    description:
      - Ensure the namespace exists and apply the validator Service, NodePort Service, and Deployment.
    options:
      committer_k8s_wait_timeout:
        type: int
        required: false
        default: 120
        description:
          - Timeout in seconds while waiting for the validator Deployment rollout.

  verifier/k8s/start:
    short_description: Start the verifier on Kubernetes
    description:
      - Ensure the namespace exists and apply the verifier Service, NodePort Service, and Deployment.
    options:
      committer_k8s_wait_timeout:
        type: int
        required: false
        default: 120
        description:
          - Timeout in seconds while waiting for the verifier Deployment rollout.

  coordinator/k8s/start:
    short_description: Start the coordinator on Kubernetes
    description:
      - Ensure the namespace exists and apply the coordinator Service, NodePort Service, and Deployment.
    options:
      committer_k8s_wait_timeout:
        type: int
        required: false
        default: 120
        description:
          - Timeout in seconds while waiting for the coordinator Deployment rollout.
      committer_validators:
        type: list
        required: true
        description:
          - Inventory hosts devoted to validator components.
      committer_verifiers:
        type: list
        required: true
        description:
          - Inventory hosts devoted to verifier components.

  sidecar/k8s/start:
    short_description: Start the sidecar on Kubernetes
    description:
      - Ensure the namespace exists and apply the sidecar Service, NodePort Service, and StatefulSet.
    options:
      committer_k8s_wait_timeout:
        type: int
        required: false
        default: 120
        description:
          - Timeout in seconds while waiting for the sidecar StatefulSet rollout.

  query_service/k8s/start:
    short_description: Start the query-service on Kubernetes
    description:
      - Ensure the namespace exists and apply the query-service Service, NodePort Service, and Deployment.
    options:
      committer_k8s_wait_timeout:
        type: int
        required: false
        default: 120
        description:
          - Timeout in seconds while waiting for the query-service Deployment rollout.

  validator/k8s/rm:
    short_description: Remove validator Kubernetes resources
    description:
      - Delete the validator Deployment and Services.
    options:
      committer_k8s_resource_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for validator resources.
      k8s_namespace:
        type: str
        required: true
        description:
          - Kubernetes namespace that contains the validator resources.

  verifier/k8s/rm:
    short_description: Remove verifier Kubernetes resources
    description:
      - Delete the verifier Deployment and Services.
    options:
      committer_k8s_resource_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for verifier resources.
      k8s_namespace:
        type: str
        required: true
        description:
          - Kubernetes namespace that contains the verifier resources.

  coordinator/k8s/rm:
    short_description: Remove coordinator Kubernetes resources
    description:
      - Delete the coordinator Deployment and Services.
    options:
      committer_k8s_resource_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for coordinator resources.
      k8s_namespace:
        type: str
        required: true
        description:
          - Kubernetes namespace that contains the coordinator resources.

  sidecar/k8s/rm:
    short_description: Remove sidecar Kubernetes resources
    description:
      - Delete the sidecar StatefulSet and Services.
    options:
      committer_k8s_resource_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for sidecar resources.
      k8s_namespace:
        type: str
        required: true
        description:
          - Kubernetes namespace that contains the sidecar resources.

  query_service/k8s/rm:
    short_description: Remove query-service Kubernetes resources
    description:
      - Delete the query-service Deployment and Services.
    options:
      committer_k8s_resource_name:
        type: str
        required: false
        default: "{{ inventory_hostname }}"
        description:
          - Base Kubernetes resource name used for query-service resources.
      k8s_namespace:
        type: str
        required: true
        description:
          - Kubernetes namespace that contains the query-service resources.

  validator/k8s/config/transfer:
    short_description: Create the validator ConfigMap
    description:
      - Ensure the namespace exists and create the validator Kubernetes ConfigMap.
    options:
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory used to derive mounted DB CA paths.

  verifier/k8s/config/transfer:
    short_description: Create the verifier ConfigMap
    description:
      - Ensure the namespace exists and create the verifier Kubernetes ConfigMap.
    options: {}

  coordinator/k8s/config/transfer:
    short_description: Create the coordinator ConfigMap
    description:
      - Ensure the namespace exists and create the coordinator Kubernetes ConfigMap.
    options:
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory used to derive mounted CA paths.
      committer_validators:
        type: list
        required: true
        description:
          - Inventory hosts devoted to validator components.
      committer_verifiers:
        type: list
        required: true
        description:
          - Inventory hosts devoted to verifier components.

  sidecar/k8s/config/transfer:
    short_description: Create the sidecar ConfigMap
    description:
      - Ensure the namespace exists and create the sidecar Kubernetes ConfigMap.
    options:
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory used to derive mounted CA paths.

  query_service/k8s/config/transfer:
    short_description: Create the query-service ConfigMap
    description:
      - Ensure the namespace exists and create the query-service Kubernetes ConfigMap.
    options:
      committer_remote_config_dir:
        type: path
        required: true
        description:
          - Remote config directory used to derive mounted DB CA paths.

  validator/teardown:
    short_description: Teardown the validator
    description:
      - Remove validator runtime resources for the active deployment mode.
    options:
      committer_use_k8s:
        type: bool
        required: false
        default: false
        description:
          - Remove validator Kubernetes resources when enabled.
      committer_use_container:
        type: bool
        required: false
        default: true
        description:
          - Remove the validator container when enabled.
      committer_use_bin:
        type: bool
        required: false
        default: false
        description:
          - Stop the validator binary when enabled.

  verifier/teardown:
    short_description: Teardown the verifier
    description:
      - Remove verifier runtime resources for the active deployment mode.
    options:
      committer_use_k8s:
        type: bool
        required: false
        default: false
        description:
          - Remove verifier Kubernetes resources when enabled.
      committer_use_container:
        type: bool
        required: false
        default: true
        description:
          - Remove the verifier container when enabled.
      committer_use_bin:
        type: bool
        required: false
        default: false
        description:
          - Stop the verifier binary when enabled.
