#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#
---
apiVersion: v1
kind: Service
metadata:
  name: {{ loki_k8s_resource_name }}-loadbalancer
  labels:
    app.kubernetes.io/name: {{ loki_k8s_resource_name }}
    app.kubernetes.io/component: loki
    app.kubernetes.io/part-of: {{ loki_k8s_part_of }}
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: {{ loki_k8s_resource_name }}
  ports:
{% if loki_k8s_loadbalancer_expose_port %}
    - name: http
      port: {{ loki_port }}
      targetPort: http
      protocol: TCP
{% endif %}
