#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#
---
apiVersion: v1
kind: Service
metadata:
  name: {{ loki_k8s_resource_name }}-nodeport
  labels:
    app.kubernetes.io/name: {{ loki_k8s_resource_name }}
    app.kubernetes.io/component: loki
    app.kubernetes.io/part-of: {{ loki_k8s_part_of }}
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: {{ loki_k8s_resource_name }}
  ports:
{% if loki_k8s_node_port is defined %}
    - name: http
      port: {{ loki_port }}
      targetPort: http
      nodePort: {{ loki_k8s_node_port }}
      protocol: TCP
{% endif %}
