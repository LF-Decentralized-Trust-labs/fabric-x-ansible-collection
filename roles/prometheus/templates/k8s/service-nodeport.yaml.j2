#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#
---
apiVersion: v1
kind: Service
metadata:
  name: {{ prometheus_k8s_resource_name }}-nodeport
  namespace: {{ k8s_namespace }}
  labels:
    app.kubernetes.io/name: {{ prometheus_k8s_resource_name }}
    app.kubernetes.io/part-of: {{ prometheus_k8s_part_of }}
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: {{ prometheus_k8s_resource_name }}
  ports:
{% if prometheus_k8s_node_port is defined %}
    - name: http
      port: {{ prometheus_port }}
      targetPort: http
      nodePort: {{ prometheus_k8s_node_port }}
{% endif %}
