#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#

- name: Check that the Postgres Exporter metrics node port is open
  vars:
    utils_ports_to_ping:
      - "{{ postgres_exporter_k8s_node_port }}"
  ansible.builtin.include_role:
    name: hyperledger.fabricx.utils
    tasks_from: ping
  when:
    - postgres_exporter_k8s_node_port is defined

- name: Check that the Postgres Exporter metrics load balancer port is open
  vars:
    utils_ports_to_ping:
      - "{{ postgres_exporter_port }}"
  ansible.builtin.include_role:
    name: hyperledger.fabricx.utils
    tasks_from: ping
  when:
    - postgres_exporter_k8s_loadbalancer_expose_metrics_port
