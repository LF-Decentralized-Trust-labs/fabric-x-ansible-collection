#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#

- name: Delete Fabric CA Kubernetes Secret
  kubernetes.core.k8s:
    state: absent
    kind: Secret
    name: "{{ fabric_ca_server_k8s_resource_name }}"
    namespace: "{{ k8s_namespace }}"
