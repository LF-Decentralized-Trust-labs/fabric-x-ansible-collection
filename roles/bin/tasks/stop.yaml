#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#

- name: Stop binary within tmux session
  vars:
    tmux_session_name: "{{ bin_name }}"
  ansible.builtin.include_role:
    name: hyperledger.fabricx.tmux
    tasks_from: stop
  when:
    - bin_run_with_tmux
