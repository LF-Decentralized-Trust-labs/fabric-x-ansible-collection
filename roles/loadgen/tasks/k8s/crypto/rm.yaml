#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#

- name: Remove Fabric-X Loadgen Secret
  kubernetes.core.k8s:
    state: absent
    kind: Secret
    name: "{{ loadgen_k8s_resource_name }}-secret"
    namespace: "{{ k8s_namespace }}"
  when: loadgen_use_tls
